# ROADMAP — The Diet App

Дорожная карта разработки. Живой документ — обновляем по мере выполнения.

> **Принцип приоритетов:** P0 = без этого продукт не продукт; P1 = делает его
> реально полезным; P2 = отлично иметь, не блокирует релиз. Внутри P0 — порядок
> сверху вниз.

---

## Где мы сейчас (август 2026)

**Что готово:**
- 🟢 **Доменная логика** (`diet/`): расчёт КБЖУ (2 формулы BMR, 5 нозологий,
  6 стадий жизни, клетчатка), гликемический индекс и метки диабета по продуктам.
- 🟢 **База данных** (`diet/db.py` + `schema.sql`): SQLite локально, слой готов
  к переключению на PostgreSQL. Таблицы products (~31 тыс.), users, diary_entries.
- 🟢 **Гликемическая нагрузка (ГН)**: порция / приём / день, проверки норм
  диабета (приём ≤ 15, день < 80). Главная фича для диабетиков.
- 🟢 **Дневник питания в БД**: порции по приёмам пищи, итоги дня, история.
- 🟢 **UI-каркас «Vivora»** (`web/`): React 18 + Vite + TS + Tailwind, **28
  экранов** (Dashboard, Diary, Search, Product, AIAssistant, Onboarding, Profile,
  Premium…), дизайн-система (палитра, типографика, тени, градиенты). Пока на
  мок-данных — интеграция с backend следующий этап.
- 🟢 **R&D векторного поиска** (ноутбук 06): сравнение моделей эмбеддингов,
  вывод — 128–256-мерные оптимальны по качеству/стоимости.
- 🟢 **Тесты**: 47 pytest-кейсов (ГН, БД, проверки).

**Чего нет (следующие шаги):**
- 🔴 **API-слоя** (backend) — UI не к чему подключаться, он работает на моках.
- 🔴 **Продакшен-БД** (поднята только локальная SQLite).
- 🔴 **Связки UI ↔ backend**: типы UI (`FoodItem`, `MealEntry`) близки к домену,
  но данные моковые.
- 🔴 **Меток продуктов для болезней** кроме диабета (ожирение/ХБП/ССЗ).
- 🔴 **Нормальных источников микроэлементов** (калий/фосфор в OFF ≈ 0%).

---

## 🗺 Дорожная карта по направлениям

### НАПРАВЛЕНИЕ A. Данные — фундамент всего

Данные — главный блокер. Без чистых микроэлементов проверки ХБП/ССЗ/подагры
бесполезны, а семантический поиск не исправит мусор в КБЖУ.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| A1 | **Очистка OFF-России**: добор КБЖУ, нормализация названий (HTML-сущности `&quot;`, дубликаты), разметка «сухое vs готовое» | P0 | ⬜ |
| A2 | **Точечный ГИ**: сейчас ГИ берётся по категории (любой белый рис = 75). Сопоставить конкретные продукты с Sydney GI DB (1881 шт.) через аккуратный матчинг | P1 | ⬜ |
| A3 | **Источник микроэлементов**: калий/фосфор/кальций/железо/вит.С ≈ 0% в OFF. Подключить USDA SR-Legacy / ФИСЦиМ (РФ-справочник) для покрытий ХБП/ССЗ | P1 | ⬜ |
| A4 | **«Сухое vs готовое»**: углеводы в БД — для сухого риса, ГИ — для варёного. Ввести флаг состояния продукта + массу готового, чтобы ГН считалась честно | P1 | ⬜ |
| A5 | **Подагра (пурины)**: новый нутриент + новая нозология. Источник пуринов — отдельный справочник (в USDA/OFF их нет) | P2 | ⬜ |
| A6 | **Региональные блюда**: сейчас это корзина-заглушка (11 482 продукта). Разобрать на нормальные категории | P2 | ⬜ |

**Критерий готовности данных:** ≥80% покрытия КБЖУ + ключевых микроэлементов
для топ-1000 повседневных продуктов РФ.

---

### НАПРАВЛЕНИЕ B. Доменная логика (diet/)

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| B1 | **Метки продуктов для болезней** кроме диабета: obesity / ckd / cvd (сейчас только текст в `notes`, см. `scripts/05_build_unified.py:14`) | P1 | ⬜ |
| B2 | **Проверки микроэлементов** для ХБП/ССЗ против реальных данных (сейчас `conditions.py:118` обещает, но нет данных — зависит от A3) | P1 | ⬜ |
| B3 | **Подагра как нозология**: `condition='gout'`, лимиты пуринов, метки продуктов (зависит от A5) | P2 | ⬜ |
| B4 | **Гестационный диабет**: отдельная логика для беременных с диабетом (сейчас просто warning о риске) | P2 | ⬜ |
| B5 | **Добор нормы** (`suggest`) поверх БД: сейчас работает с CSV-`load_foods`, переписать на `diet.db` | P1 | ⬜ |
| B6 | **Векторный поиск продуктов**: внедрить выводы ноутбука 06 — эмбеддинги 128–256-мерные, индекс (FAISS / pgvector), интеграция в `diet.db.search_products` | P1 | ⬜ |
| B7 | **Eating patterns / паттерны**: анализ рациона за неделю (однообразие, дефициты, избытки) | P2 | ⬜ |

---

### НАПРАВЛЕНИЕ C. Инфраструктура и БД

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| C1 | **Продакшен PostgreSQL**: выбрать и поднять managed-инстанс (Selectel ~292₽/мес / Timeweb от 162₽ / Cloud.ru, VK — гранты), `psycopg`, миграция | P0 | ⬜ |
| C2 | **Миграции схемы**: сейчас `schema.sql` + идемпотентный `init_db`. Ввести Alembic, когда схема начнёт меняться чаще | P2 | ⬜ |
| C3 | **Резервные копии**: автоматические бэкапы прод-БД (у большинства managed-провайдеров есть из коробки) | P1 | ⬜ |
| C4 | **CI**: GitHub Actions — прогон `pytest` на push/PR (тесты уже есть, 47 шт.) | P1 | ⬜ |

---

### НАПРАВЛЕНИЕ D. API (backend) — НОВОЕ, КРИТИЧЕСКИЙ ПУТЬ

**Сейчас API нет вообще.** Это мост между готовой логикой и готовым UI — без
него UI остаётся на моках. Стал главным приоритетом после появления UI.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| D1 | **Выбор фреймворка**: FastAPI (рекомендация — асинк, автодокументация OpenAPI, типизация) vs Flask | P0 | ⬜ |
| D2 | **Базовые эндпоинты**: `POST /users`, `GET/POST /diary`, `GET /products?search=`, `GET /day-summary` — обёртки над `diet.db` | P0 | ⬜ |
| D3 | **Эндпоинты расчётов**: `POST /calculate` (КБЖУ), `GET /checks` (проверки ГН/микроэлементов) | P0 | ⬜ |
| D4 | **Аутентификация**: регистрация/логин пользователей, сессии/токены | P1 | ⬜ |
| D5 | **Версионирование и валидация**: Pydantic-схемы, версии API (`/v1/`) | P1 | ⬜ |
| D6 | **Деплой API**: контейнер (Docker), хостинг | P1 | ⬜ |

---

### НАПРАВЛЕНИЕ E. Дизайн и UX

**Каркас и визуальный язык уже есть** (Vivora в `web/`). Теперь нужно довести
сценарии и связать с реальным доменом.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| E1 | ~~**Формат продукта**~~ | — | ✅ web (React), решено |
| E2 | **Целевая аудитория и сценарии**: диабетик 2 типа ведёт дневник? спортсмен считает БЖУ? Врач подбирает диету? Определяет приоритет фич | P0 | ⬜ |
| E3 | ~~**Визуальный язык**~~: палитра, типографика, тени, градиенты заложены в `tailwind.config.js` | — | ✅ есть база (уточнять по мере проработки экранов) |
| E4 | **UX ключевых сценариев**: онбординг → расчёт нормы → ведение дневника → проверки/предупреждения → история. Сейчас 28 экранов-прототипов, нужно связать в поток | P1 | ⬜ |
| E5 | **Дизайн-система / компоненты**: вынести переиспользуемые элементы (карточки, поля, бейджи меток диабета, ProgressRing) в библиотеку | P1 | ⬜ |
| E6 | **Адаптация под домен**: UI на английском с мок-данными → русская локализация + реальные поля (ГИ, ГН, метки diabetes_label, граммы порций) | P1 | ⬜ |
| E7 | **Доступность (a11y)**: контраст, шрифты, скринридеры — особенно для пожилых/диабетиков с проблемами зрения | P2 | ⬜ |

---

### НАПРАВЛЕНИЕ F. Фронтенд (web/)

Формат выбран (React), каркас есть. Теперь — подключение к backend и доводка.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| F1 | ~~**Выбор стека**~~ | — | ✅ React + Vite + TS + Tailwind |
| F2 | **API-клиент**: слой обращения к backend (fetch/axios), типы под ответ API, замена мок-данных в `content.ts` | P0 | ⬜ |
| F3 | **Маршрутизация**: сейчас навигация через `useState` в `App.tsx`. Ввести react-router для URL/истории/глубоких ссылок | P1 | ⬜ |
| F4 | **Состояние дневника**: хранилище (zustand/context) для дневника пользователя, синхронизация с backend | P0 | ⬜ |
| F5 | **Реальные экраны**: дневник с ГН, поиск продуктов с diabetes_label, карточка продукта с ГИ/ГН, итоги дня с проверками | P0 | ⬜ |
| F6 | **Офлайн/PWA**: локальный кэш дневника, синхронизация | P2 | ⬜ |

---

### НАПРАВЛЕНИЕ G. ML / умные фичи

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| G1 | **Продакшен векторного поиска** (из B6): FAISS/pgvector, переиндексация при обновлении базы | P1 | ⬜ |
| G2 | **Vivi — AI-ассистент**: компонент уже в UI (`AIAssistant.tsx`, `Vivi.tsx`). Подключить LLM-бэкенд для ответов по питанию/диабету | P2 | ⬜ |
| G3 | **Распознавание еды по фото**: оценка порции и состава из фотографии блюда | P2 | ⬜ |
| G4 | **NL-логгинг**: «съел тарелку борща и два куска хлеба» → разбор в записи дневника (LLM) | P2 | ⬜ |
| G5 | **Персональные рекомендации**: на основе истории рациона + диагноза | P2 | ⬜ |

---

### НАПРАВЛЕНИЕ H. Качество, релиз, эксплуатация

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| H1 | **Покрытие тестами**: расширить с текущих 47 (ГН/БД/проверки) на КБЖУ/классификацию/checks. Добавить тесты UI (Vitest/RTL) | P1 | ⬜ |
| H2 | **Логирование и метрики**: что пользователи ищут, где падают проверки | P2 | ⬜ |
| H3 | **Юридический дисклеймер**: медицинское ПО — согласия, границы ответственности | P1 | ⬜ |
| H4 | **Локализация**: UI сейчас на английском, данные RU/EN вперемешку. Единый язык UI + i18n | P1 | ⬜ |
| H5 | **MVP-релиз**: критерий готовности первой версии для реальных пользователей | P0 | ⬜ |

---

## 🎯 Рекомендуемый порядок (критический путь к MVP)

```
ФАЗА 1 — «Прочная основа» (данные + прод-БД)
  A1 (очистка OFF) → C1 (прод PostgreSQL) → C4 (CI)
  параллельно: A3 (микроэлементы), A4 (сухое/готовое)

ФАЗА 2 — «Умная логика» (доводим до ума то, что есть)
  B1 (метки болезней) → B2 (проверки ХБП/ССЗ) → B6 (векторный поиск) → B5 (добор из БД)

ФАЗА 3 — «Связка» (API — мост между UI и backend)   ← ГЛАВНОЕ СЕЙЧАС
  D1 (FastAPI) → D2/D3 (эндпоинты) → F2/F4 (API-клиент + состояние) → F5 (реальные экраны)
  ↑ UI есть, логика есть — не хватает моста между ними

ФАЗА 4 — «Доведение до продукта»
  E2 (аудитория/сценарии) → E4/E6 (UX-поток, русская локализация) → H3 (дисклеймер) → H5 (MVP-релиз)
```

**Фаза 3 — узкое место:** UI (Vivora) и доменная логика (`diet/`) уже готовы по
отдельности. Не хватает API, который их соединит. Пока его нет, UI остаётся на
моках. Это самый эффективный следующий шаг.

---

## 📌 Открытые вопросы (требуют вашего решения)

1. **Целевая аудитория** (E2): диабетики? спортсмены? врачи? Широкий или узкий?
   → Влияет на то, какие экраны/фичи приоритезировать в готовом UI.
2. **Монетизация**: бесплатное / freemium / подписка? (экран Premium уже есть в UI)
3. **Регуляторика**: позиционируем как медицинское ПО или wellness? (H3)
4. **БД-провайдер** (C1): Selectel / Timeweb / Cloud.ru / VK? Или сначала свой VPS?
5. **Язык UI**: интерфейс сейчас на английском, данные русские. Оставить EN/двуязычный или полностью RU? (H4, E6)
6. **AI-ассистент Vivi** (G2): компонент есть в UI. Делаем ли в MVP или после?

---

## 📊 Легенда статусов

- 🟢 готово | 🟡 в работе | 🔴 не начато | ⬜ из todo-пула | ✅ решено/есть база
- **P0** — критично для MVP | **P1** — важно до релиза | **P2** — после релиза
